# Interactive Avatar - 交互式虚拟人系统

## 项目简介

这是一个完整的 3D 虚拟人交互系统，让你可以创建自己的虚拟形象，并与它进行实时语音对话。系统支持多种创建方式，从文字描述到图片上传，再到预设形象选择，让每个人都能轻松拥有专属的虚拟助手。

---

## 核心功能

### 1. 用户系统

系统提供完整的账号管理功能：

- **注册与登录**：创建个人账号，所有创建的模型和对话历史都会保存在你的账户下
- **个人中心**：查看你创建的所有虚拟形象和历史对话记录
- **数据持久化**：你的模型、对话记录都会被安全保存，随时可以回顾

### 2. 创建虚拟形象（Create 页面）

这是整个流程的第一步，提供三种灵活的创建方式：

#### 方式一：文字描述生成
- 输入你想要的形象描述，比如"一个卡通机器人"或"穿西装的商务女性"
- 系统会调用 Meshy AI 服务，根据描述自动生成 3D 模型
- 如果对于生成的形象不满意，系统支持用户重试所上传图片生成的形象

#### 方式二：图片上传生成
- 上传一张图片（支持人物照片、手绘图等）
- 系统会将 2D 图片转换为 3D 模型
- 适合想要还原特定形象的场景

#### 方式三：选择预设形象
- 系统内置了多个精心设计的预设形象（如男性/女性标准形象）
- 每个预设都配有专属背景和动作库
- 点击即可直接使用，无需等待生成

### 3. 辅助绑骨（Rig Assist 页面）

生成模型后，需要为它设置骨骼结构，让虚拟人能够自然地做出各种动作。

#### 点位标注流程
系统采用 Mixamo 风格的点位辅助方式，你需要依次在模型上标记以下关键点：
- **下巴（Chin）**：面部定位点
- **腹股沟（Groin）**：躯干中心点
- **手腕（Wrists）**：左右手腕各一个
- **手肘（Elbows）**：左右手肘各一个
- **膝盖（Knees）**：左右膝盖各一个

#### 操作方式
- **左键点击**：在模型上设置当前点位
- **右键点击**：取消当前点位，重新标记
- **鼠标拖拽**：旋转视角，方便从不同角度标记
- **滚轮缩放**：调整视角距离

标记完成后，点击"应用点位并重绑骨骼"按钮，系统会自动完成骨骼绑定和蒙皮处理，并生成可交互的模型。

### 4. 语音交互（Interact 页面）

这是系统的核心功能，让你与虚拟人进行真实的语音对话。

#### 交互方式

**手势触发**：
- 系统会自动开启摄像头进行手势识别
- 向虚拟人挥手，即可启动对话会话
- 支持自动检测用户是否在场（通过人脸和手部识别）

**语音对话**：
- 直接对着麦克风说话，虚拟人会实时响应
- 采用 Qwen 实时语音模型，支持自然流畅的对话
- 系统会自动进行语音识别和合成，无需手动操作

**智能打断**：
- 如果虚拟人说话时你想插话，直接开口即可
- 系统会自动中断当前回复，开始处理你的新问题

**动作表现**：
- 虚拟人会根据对话状态自动切换动作：
  - **待机（Idle）**：等待用户说话时的自然站立姿态
  - **倾听（Listening）**：检测到用户说话时的专注姿态
  - **说话（Talking）**：虚拟人回复时的表达动作
  - **挥手（Waving）**：互动动作，可手动触发

**会话管理**：
- 用户离开（10秒未检测到人脸或手部）会自动结束会话
- 长时间静默也会自动结束，节省资源
- 每次对话都会自动保存，包括完整的对话内容和摘要

### 5. 历史记录（Dashboard 页面）

查看和管理你的所有创建记录和对话历史。

#### 模型管理
- 查看所有创建过的虚拟形象
- 显示模型来源（文字生成/图片生成/预设）
- 支持重新加载之前的模型进行交互

#### 对话历史
- 完整的对话记录，包括时间、轮次、对话内容
- 智能搜索：支持按关键词搜索历史对话
- 时间筛选：可按日期范围查看特定时期的对话
- 对话摘要：每次会话都会自动生成不超过 300 字的摘要，快速了解对话要点

#### 数据统计
- 对话轮次统计
- 用户输入和虚拟人回复的次数统计
- 会话时长记录

---

## 使用流程

完整的使用流程如下：

1. **注册/登录** → 创建账号或登录已有账号
2. **创建形象** → 选择文字描述、图片上传或预设形象
3. **辅助绑骨** → 在模型上标记关键点位，完成骨骼绑定
4. **语音交互** → 挥手启动对话，与虚拟人实时交流
5. **查看历史** → 在个人中心回顾所有模型和对话记录

---

## 特色亮点

### 智能化体验
- **自动关键词匹配**：输入描述时自动识别性别关键词，快速匹配预设
- **手势识别启动**：无需点击按钮，挥手即可开始对话
- **自动会话管理**：智能检测用户状态，自动开始和结束会话
- **实时语音交互**：无延迟的语音识别和合成，对话体验流畅自然

### 灵活的创建方式
- 支持文字、图片、预设三种输入方式
- Meshy AI 生成高质量 3D 模型
- 预设形象配有专属背景和动作库

### 完整的数据管理
- 所有模型和对话都会保存
- 支持搜索和筛选历史记录
- 自动生成对话摘要，方便回顾

### 自然的动作表现
- 根据对话状态自动切换动作
- 支持多种预设动作（待机、倾听、说话、挥手等）
- 动作与语音状态完美同步

---

## 技术优势

虽然不深入技术细节，但值得一提的是：

- **实时语音处理**：采用 WebSocket 实现低延迟的双向音频传输
- **3D 渲染**：基于 Three.js 的高性能 3D 渲染引擎
- **手势识别**：集成 MediaPipe 实现准确的手势和人脸检测
- **自动绑骨**：智能的骨骼绑定算法，支持各种模型结构
- **数据持久化**：完整的数据库支持，确保数据安全

---

## 适用场景

- **虚拟助手**：创建专属的 AI 助手，随时解答问题
- **教育培训**：制作虚拟讲师，进行互动式教学
- **客户服务**：部署虚拟客服，提供 24 小时服务
- **娱乐互动**：创建虚拟偶像或游戏角色，进行趣味对话
- **个人陪伴**：打造虚拟伙伴，进行日常交流

---

## 注意事项

- 首次使用需要允许浏览器访问摄像头和麦克风权限
- 文字和图片生成功能需要 Meshy API 配额，配额不足时会自动使用预设模型
- 建议在光线充足的环境下使用，以获得更好的手势识别效果
- 对话内容会被保存，请注意隐私保护

---

## 总结

Interactive Avatar 是一个功能完整、易于使用的虚拟人交互系统。从创建到交互，整个流程简单直观，让每个人都能轻松拥有自己的虚拟助手。无论是工作、学习还是娱乐，都能找到合适的应用场景。
